(ns odum.views
  "# Views"
  (:require [posh.reagent :as posh]
            [reagent.core :as r]
            [taoensso.timbre :as log]
            [re-com.core :as re-com]
            [datascript.core :as d]
            [posh.reagent :as posh]))



;; ## Main

;; This is the main view function, which you plug into the Datview component

(defn main [app]
  [])

